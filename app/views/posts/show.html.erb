<h1>
	<%= @post.title %><br>
	<%= @post.description %>
</h1>

<ul>
	<% @post.comments.each do |comment| %>
	<li><%= comment.body %></li>
	<% end %>
	<li>
	<%= form_for [@post, @post.comments.build] do |f| %>
	<%= f.text_field :body %>
	<%= f.submit %>
	<% end %>
	</li>
</ul>